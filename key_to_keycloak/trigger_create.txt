CREATE OR REPLACE FUNCTION notify_id_trigger() RETURNS trigger AS $$
BEGIN
  PERFORM pg_notify('new_id', NEW.ID);
  RETURN new;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER data_modified AFTER insert or update on user_entity for each row execute procedure notify_id_trigger();")

CREATE TRIGGER data_modified AFTER insert or update on user_attribute for each row execute procedure notify_id_trigger();")